# OS junk files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo

# In case any tooling gets added later
node_modules/
npm-debug.log*

# Local scratch / sample diagrams you don't want to publish
/scratch/
*.local.bpmn
deploy_esnc.expect
deploy_esnc.sftp

# Internal project notes (server IP, hosting panel details) — not for the
# public repo. Untracked from git history separately; see README/commit
# instructions for the one-time `git rm --cached` needed the first time.
KOMPENDIUM.md
